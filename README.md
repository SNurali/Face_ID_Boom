Face ID Boom 🚀

Face ID Boom — система распознавания лиц нового поколения в стиле JPG-Style Production.

Пайплайн:

📷 Фото → 🧠 Детекция и качество → 🔢 Эмбеддинг → 🗄 Хранение → 🔍 Быстрый поиск

Версия: 0.6.0
Редакция: ClickHouse + Qdrant
Статус: активная разработка (январь 2026)

🔥 Основные возможности

Асинхронный API на FastAPI

Полный face-pipeline:

детекция лица

проверка качества (blur, det_score, face_size)

извлечение embedding (InsightFace / ArcFace)

Повторная регистрация:

автоматический выбор лучшего фото

Сохранение кропа лица по bbox

Хранение данных:

ClickHouse — метаданные

Qdrant — векторный поиск

Поиск по фото с cosine similarity

Статический frontend (камера + поиск)

Docker-инфраструктура готова к продакшену

Чистая архитектура:

schemas → services → repositories → api → dependencies

🧠 Технологический стек

Backend

Python 3.11+

FastAPI

Uvicorn

ML / Face Recognition

insightface (ArcFace)

ONNX Runtime (CPU, с возможностью GPU)

Хранение

ClickHouse (Array(Float32))

Qdrant (vector DB)

Инфраструктура

Docker

docker-compose

ulimits для ClickHouse

📁 Структура проекта (упрощённо)
app/
 ├─ api/              # роутеры FastAPI
 ├─ services/         # бизнес-логика
 ├─ repositories/     # работа с БД
 ├─ schemas/          # Pydantic модели
 ├─ static/           # frontend (JS / CSS)
 ├─ templates/        # HTML
 ├─ utils/            # валидация, helpers
 └─ main.py           # точка входа

⚙️ Требования

Перед стартом убедись, что установлено:

Python 3.11+

Docker 20+

Docker Compose v2

Linux / macOS (Windows через WSL2)

🚀 Быстрый старт (ПОЛНЫЙ)
1️⃣ Клонировать репозиторий
git clone https://github.com/SNurali/Face_ID_Boom.git
cd Face_ID_Boom

2️⃣ Запустить базы данных (ClickHouse + Qdrant)
docker compose up -d


Проверить, что контейнеры запущены:

docker ps


Ты должен увидеть:

face_id_clickhouse

qdrant_faceid

3️⃣ Создать базу данных в ClickHouse (один раз)
docker exec -it face_id_clickhouse clickhouse-client


Внутри ClickHouse:

CREATE DATABASE IF NOT EXISTS face_id_boom;
SHOW DATABASES;


Выйти:

exit

4️⃣ Создать виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate

5️⃣ Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt


⚠️ При первом запуске InsightFace автоматически скачает модели (~300 МБ)

6️⃣ Запустить сервер FastAPI
uvicorn app.main:app --reload


Если всё успешно — увидишь:

Application startup complete.

🌐 Доступ после запуска

Frontend (камера + поиск):
👉 http://127.0.0.1:8000

Swagger API:
👉 http://127.0.0.1:8000/docs

🔍 Основные эндпоинты
Метод	URL	Описание
POST	/register	Регистрация человека по фото
POST	/search	Поиск по фото
GET	/docs	Swagger UI
📊 Текущий статус (январь 2026)
✅ Работает

Регистрация пользователя

Извлечение embedding с quality gates

Сохранение кропа лица

Выбор лучшего фото

ClickHouse + Qdrant

Поиск с процентом схожести

Отображение всех данных на фронте

🚧 В процессе

Оптимизация batch-поиска

Очистка старых embedding

Ролевой доступ к данным

S3 / MinIO для хранения фото

🔐 Безопасность и приватность

Фото и embedding не публикуются

Рекомендуется:

не коммитить изображения

использовать .gitignore

ограничивать поля API по ролям

🏁 Итог

Face ID Boom — это:

не демо

не туториал

а реальная production-система распознавания лиц

Проект готов к:

демонстрациям

пилотам

дальнейшему масштабированию